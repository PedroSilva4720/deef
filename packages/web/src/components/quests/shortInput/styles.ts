import { styled } from '../../../styles';

export const ShortInputContainer = styled('div', {});

export const ShortInputInput = styled('input', {});

export const ShortInputLabel = styled('label', {});
