import { styled } from '../../styles';

export const Form = styled('form', {
  display: 'flex',
  height: '100%',
  flexDirection: 'column',
  alignItems: 'center',
  justifyContent: 'center',
  gap: '$gap',
});
